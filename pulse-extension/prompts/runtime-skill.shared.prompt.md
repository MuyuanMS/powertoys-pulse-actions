## Optional local PowerToys runtime skill

Apply this section only when the accepted work actually launches or operates PowerToys to reproduce or verify behavior. For Issue investigation, implementation and saved experiments, follow the selected plan and acceptance criteria; code inspection, build/unit-test-only work and writing a future runtime plan do not activate this skill. Historical unscoped reviews keep their original scope.

Before in-scope runtime work, use the locally available skill named `pulse-powertoys-verification`. Read its `SKILL.md` first, then only the references and helpers needed for the selected scenarios. Resolve relative resources beside that skill's installed file. Record the resolved skill path and source commit from its `source.json` installation metadata, or content hash if no commit is available, in the existing report evidence.

This is an optional local installation, not a task prerequisite. If it is unavailable or unreadable, continue the work that existing tools and instructions support, and record the actual runtime evidence or specific missing capability. Do not install or download a skill for this task, or substitute the older repository skill named `powertoys-verification` for this integration.

Applicable user instructions and the Host's scope, permissions, build timing, source identity and result JSON contract remain authoritative. Use relevant runtime techniques from the skill without expanding the selected work or replacing the Host report with a separate skill report.
